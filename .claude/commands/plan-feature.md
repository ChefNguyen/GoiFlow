# /plan-feature

1. Write or update the spec in `docs/product/`
2. Identify the smallest production-meaningful slice
3. Name the touched routes, features, server modules, and tests
4. Note whether an ADR or runbook update is required
