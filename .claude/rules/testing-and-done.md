# Testing And Done

- Add tests in proportion to risk
- Keep unit and integration coverage close to the changed behavior
- Run `npm run verify` before calling a slice done
- Update docs when the change creates a durable decision or new operator workflow
