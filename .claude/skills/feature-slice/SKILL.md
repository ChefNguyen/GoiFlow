# Feature Slice

1. Update `docs/product/` with the user-facing behavior
2. Implement the smallest vertical slice across route, feature, and server layers
3. Add focused tests
4. Run the verify command
